/**
 * pes_nfc_uid.c
 *
 * UID extraction is handled inside the HAL (extract_uid in pes_nfc_hal_ptx105r.c).
 * This file is kept as a placeholder for future platform-independent UID utilities
 * (e.g. UID formatting, comparison).
 */

#include "pes_common.h"
